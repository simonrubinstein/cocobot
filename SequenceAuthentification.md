# Introduction #

Liste des fonctions appelées pour l'authentification

# Details #

```
function firsty()
{
agix(url1+"40"+mynickname+"*"+myage+mysex+parami[3]+myavatar+speco+mypass,4);
}
```

```
if(olko==12)
{
    pretsalon=true;
    var lebonnick=parseInt(urlo.substring(2,8));
    mynickID=""+lebonnick;  
    monpass=urlo.substring(8,14);
    mycrypt=parseInt(urlo.substring(14,21));
    olko=51;
}
if(olko==51)
{
    setTimeout("agir('51'+agento)",500);
}
```



```
if(olko==99)
{
  var bud=parseInt(urlo.substring(2,5));
  ...
  if(bud==556)
  { 
    minitimo();
    clearTimeout(stopserv);
    stopserv=setTimeout("lancetimer()",4000);
    if(finito>0)
    {
      ...
    }
    else 
    {
      setTimeout("agix(urlav+myage+mysex+parami[3]+myavatar+mynickID+monpass+mycrypt,4)",1800);
      var ind=0;
      var ind2=0;
      var ind3=0;
      var tro=true;
      mystat=parseInt(urlo.substring(6,7));
      myXP=parseInt(urlo.substring(5,6));
      myver=parseInt(urlo.substring(7,8));
      if(myver>3)
      {
        reche();
        document.getElementById("critero").innerHTML=rechum;
      }
      ...


  }
}
```

